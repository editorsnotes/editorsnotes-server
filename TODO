Fix display of page buttons in scan view.

Upgrade to WYMeditor 0.5rc2.

Populate new footnote forms with contents of user's clipboard.

Add to document view a list of all the citations (with notes) of that document. 

Change name of Note to Report?

Make print stylesheet for transcript with all footnotes at bottom.

Make a custom manager method for gettings Notes by Topic.

Need to get tests running again, which will require a new fixture.
Or perhaps, redo tests so as to not require fixture.
